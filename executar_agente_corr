#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
COMANDO SIMPLES: EXECUTAR AGENTE CORRIGIDO
==========================================

Usar agente existente com correção rápida
"""

import sys
import os

def main():
    print("="*60)
    print("🚀 EXECUTANDO AGENTE ADAPTIVE CORRIGIDO")
    print("="*60)
    print("🔧 Aplicando correção rápida...")
    print("   • Trailing: 5% ATR ($0.24 lucro)")
    print("   • Confirmações: 1 (vs 2)")
    print("   • Verificação: 10s (vs 15s)")
    print("="*60)
    
    try:
        # Método 1: Usar agente existente
        print("🔄 Carregando agente existente...")
        
        # Importar agente base
        sys.path.insert(0, 'src')
        from src.agents.gold_adaptive_agent import GoldAdaptiveAgent
        
        # Criar agente com parâmetros corrigidos
        agent = GoldAdaptiveAgent(
            symbol="XAUUSDc",
            volume=0.03,  # Volume maior
            trailing_activation_atr_multiplier=0.05,  # 5% ATR (corrigido)
            trailing_distance_atr_multiplier=0.02,   # 2% ATR (corrigido)
            check_interval=10,  # 10s (mais frequente)
            auto_tuning_enabled=False,  # Desabilitar auto-learning
            use_buy=True,
            use_sell=True
        )
        
        print("✅ Agente carregado com correções!")
        print("⏱️  Iniciando...")
        print("-" * 40)
        
        # Executar
        agent.run()
        
    except Exception as e:
        print(f"[❌] Erro: {e}")
        print("\n📋 SOLUÇÕES:")
        print("1. Execute: python validar_adaptive_corrigido.py")
        print("2. Verificar se MT5 está conectado")
        print("3. Usar agente original: python gold_ultra_agressivo.py")

if __name__ == "__main__":
    main()
